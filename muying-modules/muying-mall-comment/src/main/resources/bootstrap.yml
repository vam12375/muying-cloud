spring:
  application:
    name: muying-mall-comment
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      username: nacos
      password: nacos
      discovery:
        namespace: dev
        group: COMMENT_GROUP
      config:
        namespace: dev
        group: COMMENT_GROUP
        file-extension: yml
        shared-configs:
          - data-id: muying-mall-common.yml
            group: DEFAULT_GROUP
            refresh: true